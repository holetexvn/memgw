CI runs the full suite on Ubuntu and macOS across Node 20 and 22, verifies the CLI works
from a clean home directory, and checks the npm package stays under 2 MB unpacked.
No API keys are needed: the suite runs in mock mode.
